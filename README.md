# Story Scribe

A SillyTavern extension that automatically summarizes your story as it grows —
tracking plot, character dynamics, and setting details in separate layers,
then quietly storing them in World Info so the AI always has context
without eating your token budget.

## Installation

1. Unzip so you have a folder called `story-scribe`
2. Drop the folder into `SillyTavern/public/scripts/extensions/third-party/`
3. Restart / refresh SillyTavern
4. Open **Extensions** settings → look in the **right panel** for **📖 Story Scribe**

## Features (coming in later stages)

- Auto-summarize every N messages
- Separate summaries for: Plot, Characters, Setting
- Saves summaries into a World Info entry automatically
- Manual edit before saving so you stay in control
- Token budget display so you always know where you stand
